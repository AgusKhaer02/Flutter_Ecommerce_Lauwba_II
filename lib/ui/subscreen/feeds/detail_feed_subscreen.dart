import 'package:flutter/material.dart';

class DetailFeedSubscreen extends StatelessWidget {
  const DetailFeedSubscreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

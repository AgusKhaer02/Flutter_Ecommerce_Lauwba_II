import 'package:flutter/material.dart';

class DetailHelpSubscreen extends StatelessWidget {
  const DetailHelpSubscreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
